#include <stdio.h>

int main()
{
  float x,y,media;

  printf("Entre com a 1° nota:");
  scanf("%f", &x);
  printf("Entre com a 2° nota:");
  scanf("%f", &y);
  media= (x+y)/2;
  printf("A média das notas é:%f",media);
return 0;
}
  
